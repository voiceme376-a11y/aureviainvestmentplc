# Aurevia v13 — Administrator Live Trade Feed

Server-side admin trade publishing, user assignment, current-price/status updates, persistent candles and a custom Aurevia 4D-style canvas candlestick view. No fake customer accounts or balances are seeded.

Apply `migrations/0005_live_trade_control.sql` to the same D1 database before using the live trade controls.
