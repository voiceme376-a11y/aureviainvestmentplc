# Aurevia Investment PLC — v12

This release fixes the `Request failed` account-creation issue and keeps the full Aurevia account/dashboard UI operational when a deployment has not yet received its D1 binding.

Use the root `index.html` entry point. For production server-backed accounts, bind D1 as `DB` and redeploy.
